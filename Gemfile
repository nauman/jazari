# frozen_string_literal: true

source "https://rubygems.org"
gemspec

group :development, :test do
  gem "rake"
  gem "minitest"
  gem "pg"          # Postgres-only: the suite runs the migration the gem ships
  gem "rubocop-rails-omakase", require: false
end
